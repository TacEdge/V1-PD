// Page Definition Sheets — one per meaningful active V1 screen.
// Small variants share a sheet; every route is listed in the appendix inventory.
// No screenshots were captured: the application could not be run safely during
// inspection (see the methodology note in section 00).
export interface Screen {
  id: string;
  name: string;
  routes: string[];
  role: string;
  surface: string;
  classification: string;
  job: string;
  userQuestion: string;
  entry: string;
  exits: string;
  hierarchy: string;
  components: string;
  inputs: string;
  actions: string;
  outputs: string;
  states: string; // validation / empty / loading / error / permissions in prose
  reads: string;
  writes: string;
  dependencies: string;
  limitations: string;
  capabilities: string[]; // capability IDs
  evidence: string;
  confidence: 'High' | 'Medium' | 'Low';
  validation: string;
}

export const screens: Screen[] = [
  {
    id: 'PDS-DSH', name: 'Dashboard', routes: ['/'], role: 'All roles (widgets vary by role group)',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Give each role a live summary of projects, forms, incidents and activity on login.',
    userQuestion: '"What is happening across my work right now?"',
    entry: 'Default authenticated route; sidebar Home; mobile bottom-nav Home.',
    exits: 'Into projects, forms, incidents and meetings via widget links.',
    hierarchy: 'Primary: role-specific stat widgets. Secondary: active jobs / recent forms. Tertiary: incident alerts.',
    components: 'Role-gated widget set from rbac-config DASHBOARD_WIDGETS; LPC tenants render the bespoke LPCDashboard instead.',
    inputs: 'None (read-only).', actions: 'Navigate; refresh.',
    outputs: 'None.',
    states: 'Loading skeletons via React Query; management-overview endpoint restricted to management roles server-side. Empty/error states not exhaustively traced.',
    reads: 'GET /api/dashboard/management-overview, /stats, /my-activity', writes: '—',
    dependencies: 'dashboard.ts routes (SQL aggregate stats)',
    limitations: 'Two inline useQuery calls (extraction debt). LPC dashboard bypasses the design tokens.',
    capabilities: ['V1-DSH-01', 'V1-DSH-02'], evidence: 'client/src/pages/dashboard.tsx (322 lines); server/routes/dashboard.ts',
    confidence: 'High', validation: 'Widget correctness per role unverified at runtime.',
  },
  {
    id: 'PDS-PRJ', name: 'Projects list', routes: ['/projects'], role: 'Management / Supervisor / Viewer',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'List, create and open projects (relabelled "Departments" for the port tenant).',
    userQuestion: '"Which projects exist and what state are they in?"',
    entry: 'Sidebar; dashboard links.', exits: 'Project detail; create-project dialog.',
    hierarchy: 'Primary: project cards/list with status. Secondary: create action. Tertiary: SharePoint sync (feature-flagged).',
    components: 'useProjects hook; project cards; status badges.',
    inputs: 'New project details.', actions: 'Create project; open project; sync SharePoint (flag).',
    outputs: 'projects rows.',
    states: 'Create gated by canManageJobs (write). Port tenants see relabelled copy.',
    reads: 'GET /api/projects', writes: 'POST /api/projects',
    dependencies: 'projects.ts routes', limitations: '',
    capabilities: ['V1-PRJ-01'], evidence: 'client/src/pages/projects.tsx (410 lines)',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-PRJD', name: 'Project detail workspace', routes: ['/projects/:id'], role: 'Management / Supervisor',
    surface: 'Desktop-first, responsive', classification: 'IMPLEMENTED',
    job: 'The project hub: tabs for anchors, designs/setup, face photos, map/surveys, documents, reports, DAS specs, meetings and team.',
    userQuestion: '"What is the full state of this project?"',
    entry: 'Projects list; dashboard; global search.',
    exits: 'Drill Summary, Report Builder, survey viewers, face-photo canvas.',
    hierarchy: 'Primary: tab strip + active tab content. Secondary: project header with status/code. Tertiary: setup dialogs.',
    components: 'useProjectNav; ProjectDesignTab (784 lines); ProjectReportsTab (842); FacePhotoAnchorMap; MapSurvey; job-brief components.',
    inputs: 'Setup entities (designs, mixes, methods, zones, sections); uploads.',
    actions: 'Configure setup; plan anchors (manual or AI import); manage team; upload documents/photos.',
    outputs: 'Setup rows; planned anchors; documents.',
    states: 'Write actions gated by canManageJobs; anchor-import restricted to OrgAdmin/PM roles.',
    reads: 'GET /api/projects/:id, /config, face-photos, surveys, reports', writes: 'Setup CRUD; planned anchors; imports',
    dependencies: 'anchor-logs/config.ts; anchor-import.ts; job-brief.ts',
    limitations: 'Largest composite surface in the app; several child components exceed 500 lines.',
    capabilities: ['V1-PRJ-02', 'V1-CFG-01', 'V1-CFG-02', 'V1-CFG-03', 'V1-PLN-01', 'V1-PLN-02', 'V1-MAP-02'],
    evidence: 'client/src/pages/project-detail.tsx (452 lines) + project/ components',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-DRL', name: 'Drill (field capture)', routes: ['/drill'], role: 'Field crew',
    surface: 'Phone/tablet-first', classification: 'IMPLEMENTED',
    job: 'Capture drill and grout logs against planned anchors in a grid built for gloves-on field entry.',
    userQuestion: '"Which anchor am I on, and what do I need to record?"',
    entry: 'Sidebar (Field group); mobile bottom-nav Drill button.',
    exits: 'Anchor detail dialogs; Drill Summary (management).',
    hierarchy: 'Primary: anchor grid by zone with status colours. Secondary: quick-fill dialogs. Tertiary: notes/issues.',
    components: 'useDrill hook; DrillerQuickFillDialog; GrouterQuickFillDialog; active-zone persistence (localStorage).',
    inputs: 'Drill method, depths, dates, lithology layers, grout mix/volumes, issues.',
    actions: 'Record drill; record grout; bulk drill/grout; flag issues.',
    outputs: 'anchor_install_logs updates; ground_profile_layers.',
    states: 'Offline drafts persist via IndexedDB/localStorage; sync queue replays on reconnect. Status auto-advances planned→drilled→grouted.',
    reads: 'GET /api/projects/:id/anchor-logs, /config', writes: 'POST/PATCH anchor-logs; bulk endpoints',
    dependencies: 'offline-store.ts; anchor-logs routes',
    limitations: '797 lines with inline logic; offline replay behaviour unverified at runtime.',
    capabilities: ['V1-DRL-01', 'V1-DRL-02', 'V1-DRL-03', 'V1-GRT-01', 'V1-OFF-01'],
    evidence: 'client/src/pages/drill.tsx; client/src/hooks/useDrill.ts',
    confidence: 'High', validation: 'Field offline capture end-to-end.',
  },
  {
    id: 'PDS-ALG', name: 'Drill Summary (anchor logs review)', routes: ['/anchor-logs'], role: 'Management / Supervisor',
    surface: 'Desktop-first', classification: 'IMPLEMENTED',
    job: 'Review all anchor install logs across a project: filter, bulk-edit, flag for PM review, export XLSX and email the log set.',
    userQuestion: '"Where is drilling up to, and is the record complete?"',
    entry: 'Sidebar Project Operations.', exits: 'Anchor detail dialogs; XLSX download; email flow.',
    hierarchy: 'Primary: log table with status/test columns. Secondary: filters + bulk actions. Tertiary: export/email.',
    components: 'useAnchorLogsPage; AnchorLogsPdfDocument; archive list/restore.',
    inputs: 'Filters; bulk edits; recipient selection.',
    actions: 'Bulk update; archive/restore; export XLSX; email logs to a teammate.',
    outputs: 'XLSX file; email via Resend; log updates.',
    states: 'Writes gated by canManageForms; email recipients restricted to tenant teammates (zod-validated).',
    reads: 'GET /api/projects/:id/anchor-logs; /api/org/teammates', writes: 'PATCH anchor-logs; POST /api/anchor-logs/email',
    dependencies: 'drill-log-xlsx.ts (Python-built template)', limitations: '950 lines; 2 inline queries.',
    capabilities: ['V1-DRL-03', 'V1-RPT-03'], evidence: 'client/src/pages/anchor-logs.tsx; server/routes/anchor-logs/',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-TST', name: 'Anchor Testing', routes: ['/anchor-testing'], role: 'Management / Supervisor / Viewer',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Record and review anchor load tests: readings per load step, photo OCR of paper sheets, AI conformance analysis, exports.',
    userQuestion: '"Did this anchor pass, against which standard?"',
    entry: 'Sidebar (opt-in feature flag).', exits: 'Test XLSX / PDF exports.',
    hierarchy: 'Primary: test list with conformance. Secondary: test entry with dial readings. Tertiary: AI OCR/analysis panel.',
    components: 'useAnchorTestingPage; test photo upload.',
    inputs: 'Load steps, dial readings (1-3 gauges), standard, jack asset, photos.',
    actions: 'Create/edit test; OCR photo; run AI analysis; export.',
    outputs: 'anchor_test_results + readings; XLSX; engineering PDF.',
    states: 'Writes gated by canManageForms; AI endpoints require auth.',
    reads: 'GET /api/anchor-tests; /api/anchor-logs/:logId/test-result', writes: 'POST/PATCH test-result; test-photo-ocr',
    dependencies: 'ai-test-analyzer.ts (Claude); assets (jack calibration)',
    limitations: 'Paper remains the primary capture medium; the platform digitises downstream.',
    capabilities: ['V1-TST-01', 'V1-TST-02', 'V1-TST-03', 'V1-TST-04'],
    evidence: 'client/src/pages/anchor-testing.tsx; server/routes/anchor-tests/',
    confidence: 'High', validation: 'OCR accuracy with real sheets.',
  },
  {
    id: 'PDS-DAS', name: 'Daily Activity Sheets', routes: ['/daily-activity'], role: 'Field / Supervisor',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Capture per-shift crew and plant hours against a project, auto-totalled, exportable as PDF and invoice XLSX.',
    userQuestion: '"Who and what worked today, for how long?"',
    entry: 'Sidebar Project Operations (hidden for port).', exits: 'PDF/XLSX downloads.',
    hierarchy: 'Primary: sheet editor (crew rows, plant rows). Secondary: sheet list by date. Tertiary: export actions.',
    components: 'TimesheetDAS; DAS crew/plant pickers fed by das_* specs.',
    inputs: 'Crew hours, plant hours, shift metadata.',
    actions: 'Create/edit sheet; export PDF; export invoice XLSX.',
    outputs: 'forms rows (type=daily-activity); PDF; XLSX.',
    states: 'DAS specs gating differs (feature flag vs !isPortOnly) — two mechanisms.',
    reads: 'GET /api/daily-activity-sheets; das-config/crew/plant', writes: 'POST/PATCH sheets',
    dependencies: 'das-specs.ts; pdfkit; xlsx', limitations: '1111 lines — largest page in the codebase.',
    capabilities: ['V1-DAS-01', 'V1-DAS-02', 'V1-DAS-03'],
    evidence: 'client/src/pages/daily-activity.tsx; server/routes/daily-activity.ts',
    confidence: 'High', validation: 'Live invoice usage.',
  },
  {
    id: 'PDS-SAF', name: 'Safety report lists', routes: ['/incidents', '/near-misses', '/site-inspections', '/asset-assessments'],
    role: 'All safety roles', surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Four sibling list screens over form submissions of a given type, each launching the matching form template.',
    userQuestion: '"What has been reported, and what needs action?"',
    entry: 'Sidebar Safety group.', exits: 'Fill-form for new reports; submission detail dialog.',
    hierarchy: 'Primary: submission list with status badges. Secondary: new-report action. Tertiary: filters.',
    components: 'Shared list pattern; SubmissionDetailDialog (812 lines).',
    inputs: 'New report via template.', actions: 'Create report; open detail; change status.',
    outputs: 'forms rows of the respective type.',
    states: 'Uses /api/form-templates/available to find the right template; inline apiRequest writes (extraction debt).',
    reads: 'GET /api/incident-reports | /near-misses | /site-inspections | /asset-assessments', writes: 'Form submissions',
    dependencies: 'forms.ts routes', limitations: 'Four near-duplicate pages sharing an unextracted pattern.',
    capabilities: ['V1-SAF-01', 'V1-SAF-02', 'V1-SAF-03'],
    evidence: 'client/src/pages/incidents.tsx / near-misses.tsx / site-inspections.tsx / asset-assessments.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-AST', name: 'Asset Register', routes: ['/assets'], role: 'Management / Supervisor',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Maintain the register of jacks, gauges, vehicles, plant and safety equipment with certification/calibration expiry.',
    userQuestion: '"Is this equipment certified and in service?"',
    entry: 'Sidebar Safety group.', exits: 'Asset detail; expiring-assets view.',
    hierarchy: 'Primary: asset list with expiry indicators. Secondary: asset editor. Tertiary: attachments.',
    components: 'useAssets hook.',
    inputs: 'Asset details, cert dates, calibration values, attachments.',
    actions: 'CRUD assets; view expiring.',
    outputs: 'assets rows.', states: 'Authenticated; categories are free text.',
    reads: 'GET /api/assets; /assets/expiring', writes: 'POST/PATCH/DELETE assets',
    dependencies: '', limitations: '',
    capabilities: ['V1-AST-01'], evidence: 'client/src/pages/assets.tsx; server/routes/assets.ts',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-MTG', name: 'Meetings list + recorder', routes: ['/meetings'], role: 'All roles',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'List safety meetings/toolbox talks and start recorded meetings with live transcription.',
    userQuestion: '"What meetings are planned or done, and can I start one now?"',
    entry: 'Sidebar Safety group (SteepWorks re-points its sign-in item elsewhere).',
    exits: 'Meeting detail.',
    hierarchy: 'Primary: meeting list with status. Secondary: start/record action. Tertiary: stats.',
    components: 'MeetingRecorder (IndexedDB audio buffering); useMeetings.',
    inputs: 'Meeting metadata; microphone audio.',
    actions: 'Create meeting; record; upload audio.',
    outputs: 'safety_meetings rows; audio files; transcripts.',
    states: 'Recording disabled per-tenant via meetingVoiceRecording flag; capability canManageMeetings for writes.',
    reads: 'GET /api/safety-meetings', writes: 'POST meetings; POST /:id/audio',
    dependencies: 'Deepgram; recording-idb.ts', limitations: '644 lines.',
    capabilities: ['V1-MTG-01', 'V1-MTG-02'], evidence: 'client/src/pages/meetings.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-MTGD', name: 'Meeting detail', routes: ['/meetings/:id'], role: 'All roles',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Run and review a meeting: attendance with signatures, tasks, photos, transcript and AI analysis, with live refresh.',
    userQuestion: '"Who attended, what was decided, and what actions came out?"',
    entry: 'Meetings list.', exits: 'Export; incident links from tasks.',
    hierarchy: 'Primary: meeting record + transcript/analysis. Secondary: attendance list. Tertiary: tasks/photos.',
    components: 'useMeetingDetail; AttendanceList; MeetingDialogs; MeetingAnalysis.',
    inputs: 'Attendance signatures; tasks; questions to ask-AI.',
    actions: 'Sign attendance; add tasks; run AI analysis; ask AI; export.',
    outputs: 'Participants/tasks/photos rows; aiAnalysis jsonb.',
    states: 'Live view via refetchInterval; audio delete restricted to management roles.',
    reads: 'GET /api/safety-meetings/:id + sub-resources', writes: 'Participants/tasks/photos; analyze; transcript',
    dependencies: 'meeting-ai-analyzer.ts (Claude); OpenAI Realtime session', limitations: '577 lines.',
    capabilities: ['V1-MTG-01', 'V1-MTG-03', 'V1-MTG-04'], evidence: 'client/src/pages/meeting-detail.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-FRM', name: 'Forms gallery + fill form', routes: ['/forms', '/forms/:id'], role: 'Field crew / all roles',
    surface: 'Phone-first', classification: 'IMPLEMENTED',
    job: 'Choose an available form template and complete it with dynamic fields, attachments and signatures.',
    userQuestion: '"Which form do I need, and what must I fill in?"',
    entry: 'Sidebar Admin/Forms; safety list screens; SteepWorks daily sign-in nav item.',
    exits: 'Submissions; JSA sign-in child flows.',
    hierarchy: 'Primary: template gallery / dynamic field renderer. Secondary: attachments + signature. Tertiary: project/asset linkage.',
    components: 'dynamic-form-renderer; SignaturePad.',
    inputs: 'Field values per template schema; attachments; signatures.',
    actions: 'Submit form; save draft (offline).',
    outputs: 'forms rows; form attachments.',
    states: 'Drafts survive offline via IndexedDB form-drafts store; zod insert-schema validation server-side.',
    reads: 'GET /api/form-templates/available; /api/forms/:id', writes: 'POST /api/forms; attachments',
    dependencies: 'form_templates.schema jsonb', limitations: 'formData shape varies by template (unstructured jsonb).',
    capabilities: ['V1-FRM-01', 'V1-FRM-02', 'V1-SAF-04', 'V1-OFF-01'],
    evidence: 'client/src/pages/forms-list.tsx, fill-form.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-FB', name: 'Form Builder', routes: ['/form-builder'], role: 'Management',
    surface: 'Desktop-only', classification: 'IMPLEMENTED',
    job: 'Author custom form templates with drag-and-drop fields across 11 form types.',
    userQuestion: '"What should this form ask, in what order?"',
    entry: 'Sidebar Admin (default-on flag; flipped on for SteepWorks by migration 0046).',
    exits: 'Forms gallery.',
    hierarchy: 'Primary: field canvas. Secondary: field palette (7 types). Tertiary: template settings (daily sign-in toggle).',
    components: '@dnd-kit sortable builder.',
    inputs: 'Template structure.', actions: 'Create/edit/delete templates.',
    outputs: 'form_templates rows.',
    states: 'Gated by canManageForms.',
    reads: 'GET /api/form-templates', writes: 'POST/PATCH/DELETE form-templates',
    dependencies: '', limitations: '574 lines.',
    capabilities: ['V1-FRM-01'], evidence: 'client/src/pages/form-builder.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-SUB', name: 'Submissions inbox', routes: ['/submissions'], role: 'All roles',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Review submitted forms across types with status lifecycle and archive filter.',
    userQuestion: '"What has been submitted and what state is it in?"',
    entry: 'Sidebar Admin.', exits: 'Submission detail dialog; PDF render of a submission.',
    hierarchy: 'Primary: submission list. Secondary: detail dialog with attachments. Tertiary: archive filter.',
    components: 'SubmissionDetailDialog (812 lines); FormSubmissionPdf.',
    inputs: 'Status changes.', actions: 'Approve/reject/archive; export submission PDF.',
    outputs: 'forms status updates.',
    states: 'Status: draft→pending→approved/rejected→completed→archived.',
    reads: 'GET /api/forms?includeArchived=true', writes: 'PATCH /api/forms/:id',
    dependencies: '', limitations: '3 inline queries.',
    capabilities: ['V1-FRM-03'], evidence: 'client/src/pages/submissions.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-VAR', name: 'Variations', routes: ['/variations'], role: 'Management / Supervisor',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Raise contract variations with reason and status workflow; Teams notification on submission.',
    userQuestion: '"What changed against the contract, and has the client approved it?"',
    entry: 'Sidebar Project Operations (opt-in flag).', exits: 'Project variations list.',
    hierarchy: 'Primary: variation form. Secondary: status workflow. Tertiary: linkage to project.',
    components: 'VariationFormBuilder.',
    inputs: 'Variation details, reason.', actions: 'Draft; submit; approve/reject.',
    outputs: 'variations rows; Teams card.',
    states: 'draft→submitted→approved/rejected.',
    reads: 'GET /api/projects/:id/variations', writes: 'POST /api/variations',
    dependencies: 'microsoftTeams.ts', limitations: '',
    capabilities: ['V1-VAR-01'], evidence: 'client/src/pages/variation-form.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-RPT', name: 'Project reports + Report Builder', routes: ['/projects/:projectId/reports', '/reports/:reportId'],
    role: 'Management', surface: 'Desktop-only (builder)', classification: 'IMPLEMENTED',
    job: 'Create engineer-facing reports from skeleton templates in a block editor, with AI-proposed sections, revision snapshots and signatories.',
    userQuestion: '"Is the client report complete, reviewed and issuable?"',
    entry: 'Project detail Reports tab; project reports list.',
    exits: 'PDF render; revision history.',
    hierarchy: 'Primary: Tiptap block editor. Secondary: data-bundle blocks (drill/test data). Tertiary: status + revision panel.',
    components: 'ReportEditor; blocks/data-blocks (760 lines); reports/theme.ts.',
    inputs: 'Report content; signatories.', actions: 'Edit; AI propose; snapshot revision; change status.',
    outputs: 'reports + report_revisions rows; PDF.',
    states: 'Text state machine enforced at route layer; revisions immutable.',
    reads: 'GET /api/reports/:id, /data-bundle, /revisions', writes: 'PATCH report; POST ai-propose; revisions',
    dependencies: '@tiptap; @react-pdf/renderer; ai-report-proposer.ts',
    limitations: 'Status not a DB enum; builder is desktop-oriented.',
    capabilities: ['V1-RPT-01', 'V1-RPT-02'], evidence: 'client/src/pages/reports.tsx, report-builder.tsx; migration 0043',
    confidence: 'High', validation: 'Whether reports are issued to clients in practice.',
  },
  {
    id: 'PDS-DOC', name: 'Documents (SharePoint browser)', routes: ['/documents'], role: 'Management / Viewer',
    surface: 'Desktop-first', classification: 'IMPLEMENTED',
    job: 'Browse, search and download SharePoint drive content via Microsoft Graph.',
    userQuestion: '"Where is the project document I need?"',
    entry: 'Sidebar (opt-in flag).', exits: 'File downloads.',
    hierarchy: 'Primary: drive/folder tree. Secondary: search. Tertiary: download actions.',
    components: 'useDocuments.',
    inputs: 'Search queries.', actions: 'Browse; search; download.',
    outputs: '—.',
    states: 'Requires tenant Azure/SharePoint configuration; read capability canManageJobs.',
    reads: 'GET /api/documents/drives/*', writes: '—',
    dependencies: 'microsoft-graph.ts; SHAREPOINT_* env', limitations: 'Distinct from per-project project_documents store — two document surfaces.',
    capabilities: ['V1-DOC-01'], evidence: 'client/src/pages/documents.tsx; server/routes/documents.ts',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-CAL', name: 'Calendar', routes: ['/calendar'], role: 'Management / Viewer',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Manage events with optional Outlook sync (port tenant uses it for vessel/maintenance scheduling).',
    userQuestion: '"What is scheduled?"',
    entry: 'Sidebar (opt-in flag).', exits: '—.',
    hierarchy: 'Primary: calendar grid. Secondary: event editor. Tertiary: Outlook connect status.',
    components: 'useCalendar.',
    inputs: 'Events; OAuth consent.', actions: 'CRUD events; connect/disconnect Outlook.',
    outputs: 'calendar_events rows; Outlook events.',
    states: 'Event CRUD gated by canManageIntegrations; public OAuth callback route.',
    reads: 'GET /api/calendar/status, events', writes: 'POST/PATCH/DELETE events',
    dependencies: 'MS Graph delegated OAuth', limitations: '',
    capabilities: ['V1-CAL-01'], evidence: 'client/src/pages/calendar.tsx; server/routes/calendar.ts',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-NTS', name: 'Notes', routes: ['/notes'], role: 'Field / Supervisor',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Keep project-scoped markdown notes with attachments, visibility and pinning.',
    userQuestion: '"What did we observe that does not fit a form?"',
    entry: 'Sidebar Project Operations.', exits: '—.',
    hierarchy: 'Primary: note list/editor. Secondary: project filter. Tertiary: pin/visibility controls.',
    components: 'useNotes.',
    inputs: 'Markdown, attachments.', actions: 'CRUD notes; pin.',
    outputs: 'notes rows.',
    states: 'Authenticated.',
    reads: 'GET /api/notes', writes: 'POST/PATCH/DELETE notes',
    dependencies: '', limitations: 'Attachments stored inline as data URLs in jsonb.',
    capabilities: ['V1-NOT-01'], evidence: 'client/src/pages/notes.tsx',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-JOB', name: 'Jobs list + job by code', routes: ['/jobs', '/jobs/:code'], role: 'Field / Supervisor',
    surface: 'Responsive', classification: 'PARTIAL',
    job: 'List H&S jobs and look one up by its short code (the pre-project grouping unit).',
    userQuestion: '"Which job am I on?"',
    entry: 'Sidebar.', exits: 'Job detail.',
    hierarchy: 'Primary: job list with progress/status. Secondary: code lookup.',
    components: 'useJobsList.',
    inputs: 'Job details (create gated).', actions: 'Open job; create (canManageJobs).',
    outputs: 'jobs rows.',
    states: 'Overlaps with projects; retained for H&S flows.',
    reads: 'GET /api/jobs; /api/jobs/code/:code', writes: 'POST/PATCH jobs',
    dependencies: '', limitations: 'Semi-legacy grouping unit.',
    capabilities: ['V1-LEG-02'], evidence: 'client/src/pages/jobs.tsx, job-detail-by-code.tsx',
    confidence: 'High', validation: 'Which tenants still run on jobs.',
  },
  {
    id: 'PDS-SIT', name: 'Sites list + site detail', routes: ['/sites', '/sites/:id'], role: 'Management / Supervisor',
    surface: 'Responsive', classification: 'PARTIAL',
    job: 'Manage sites with contacts and files from the earlier site-centric model.',
    userQuestion: '"What sites do we operate, with whom?"',
    entry: 'Sidebar.', exits: 'Site detail with map.',
    hierarchy: 'Primary: site list. Secondary: detail with contacts/files. Tertiary: map.',
    components: 'useSiteDetail.',
    inputs: 'Site details, contacts, files.', actions: 'CRUD (canManageJobs).',
    outputs: 'sites/site_contacts/site_files rows.',
    states: 'Semi-legacy; still fully routed.',
    reads: 'GET /api/sites', writes: 'POST/PATCH sites; contacts; files',
    dependencies: '', limitations: 'Superseded in practice by projects.',
    capabilities: ['V1-LEG-01'], evidence: 'client/src/pages/sites.tsx, site-detail.tsx',
    confidence: 'High', validation: 'Which tenants still use sites.',
  },
  {
    id: 'PDS-SET', name: 'Settings', routes: ['/settings'], role: 'OrgAdmin (canManageUsers)',
    surface: 'Desktop-first', classification: 'IMPLEMENTED',
    job: 'Administer the organisation: users, role-permission overrides, branding, integrations, DAS specs and org settings.',
    userQuestion: '"How is this organisation configured?"',
    entry: 'Sidebar Admin (capability-gated); user dropdown.',
    exits: '—.',
    hierarchy: 'Primary: settings tabs. Secondary: user table + invites. Tertiary: branding uploads / RBAC matrix editor.',
    components: 'BrandingTab; IntegrationsTab; role-permissions editor.',
    inputs: 'User accounts; capability overrides; branding assets; integration config.',
    actions: 'Manage users; edit RBAC overrides; upload branding; configure SharePoint.',
    outputs: 'users; role_capability_overrides; tenant_config; sharepoint_config.',
    states: 'Nav gated by canManageUsers; page itself not router-gated (server enforces).',
    reads: 'GET /api/settings/*', writes: 'PATCH settings endpoints',
    dependencies: '', limitations: 'PATCH /api/tenant/config lacks zod validation; DAS specs tab hidden for port.',
    capabilities: ['V1-USR-01', 'V1-USR-02', 'V1-ORG-02'], evidence: 'client/src/pages/settings.tsx (127 lines + tabs)',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-PRF', name: 'Profile + Help', routes: ['/profile', '/help'], role: 'All roles',
    surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Edit own profile, install the PWA, and read tenant-aware help content.',
    userQuestion: '"How do I manage my account / get help?"',
    entry: 'User dropdown in sidebar footer.', exits: '—.',
    hierarchy: 'Primary: profile fields / help sections. Secondary: PWA install. Tertiary: support contacts (tenant).',
    components: 'useProfile; useTenant.',
    inputs: 'Profile fields.', actions: 'Update profile; install app.',
    outputs: 'users updates.',
    states: 'beforeinstallprompt suppressed globally; manual install from Profile.',
    reads: 'GET /api/auth/user', writes: 'PATCH /api/user/profile',
    dependencies: '', limitations: '',
    capabilities: ['V1-OFF-01'], evidence: 'client/src/pages/profile.tsx (407), help.tsx (422)',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-AUTH', name: 'Authentication screens', routes: ['/login', '/register', '/forgot-password', '/reset-password', '/request-access', '(forced) change-password'],
    role: 'Public / all users', surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Sign in (password, Microsoft), self-register, recover access and rotate forced passwords, all tenant-branded pre-login.',
    userQuestion: '"How do I get in?"',
    entry: 'Unauthenticated root (custom tenants); /login.',
    exits: 'Dashboard on success.',
    hierarchy: 'Primary: credential form. Secondary: OAuth button. Tertiary: recovery links.',
    components: 'Tenant branding via /api/tenant/branding-by-domain (public).',
    inputs: 'Credentials; emails.', actions: 'Login; register; reset; request access.',
    outputs: 'Sessions; users; access_requests.',
    states: 'Rate-limited 10/15 min; mustChangePassword forces the change screen.',
    reads: 'branding-by-domain', writes: 'auth endpoints',
    dependencies: 'passwordAuth; microsoftAuth; Resend', limitations: 'LPC branding deliberately never returned by the public endpoint.',
    capabilities: ['V1-AUTH-01', 'V1-AUTH-02', 'V1-AUTH-04', 'V1-AUTH-05'],
    evidence: 'client/src/pages/login.tsx et al.; server/routes/auth.ts',
    confidence: 'High', validation: '',
  },
  {
    id: 'PDS-UPL', name: 'Public form upload', routes: ['/forms/:id/upload'], role: 'External / field',
    surface: 'Phone-first', classification: 'IMPLEMENTED',
    job: 'Let a device (possibly unauthenticated in the SPA) attach files to a specific form via a tokenised flow.',
    userQuestion: '"How do I get this photo onto the form?"',
    entry: 'Link/QR from a form.', exits: '—.',
    hierarchy: 'Primary: upload dropzone. Secondary: form context.',
    components: 'useTenant.',
    inputs: 'Files.', actions: 'Upload.',
    outputs: 'Form attachments.',
    states: 'Registered in both authed and unauthed router branches; server-side attachment routes are authenticated — the reconciliation (FORM_UPLOAD_SECRET) needs runtime confirmation.',
    reads: 'GET /api/forms/:id', writes: 'POST /api/forms/:id/attachments',
    dependencies: 'form-attachments.ts', limitations: 'Auth model for the unauthenticated path unclear from static reading.',
    capabilities: ['V1-FRM-02'], evidence: 'client/src/pages/form-upload.tsx; client/src/App.tsx:111,177',
    confidence: 'Medium', validation: 'Confirm the unauthenticated upload flow end-to-end.',
  },
  {
    id: 'PDS-MKT', name: 'Marketing site (9 pages)', routes: ['/ (landing)', '/about', '/geotech', '/geospatial', '/pricing', '/case-studies', '/contact', '/experience', '/try'],
    role: 'Public (TacEdge brand only)', surface: 'Responsive', classification: 'IMPLEMENTED',
    job: 'Present TACEDGE marketing content and capture contact/trial enquiries, inside the product SPA.',
    userQuestion: '"What does TACEDGE offer?"',
    entry: 'Public web on tacedgepm.com.', exits: 'Login / register / contact submission.',
    hierarchy: 'Primary: marketing layout with hero/features. Secondary: contact + trial forms.',
    components: 'MarketingLayout; MarketingNav; LandingHero.',
    inputs: 'Contact/trial details.', actions: 'Submit contact form.',
    outputs: 'Email via POST /api/contact.',
    states: 'Gated by brandName === "TacEdge"; custom tenants never see these.',
    reads: '—', writes: 'POST /api/contact',
    dependencies: 'contact.ts; Resend', limitations: 'Marketing bundled inside the product app (customer-specific).',
    capabilities: ['V1-MKT-01'], evidence: 'client/src/pages/marketing/*; App.tsx:88',
    confidence: 'High', validation: '',
  },
];
